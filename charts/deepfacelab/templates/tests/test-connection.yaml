apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deepfacelab.fullname" . }}-test-connection"
  labels:
    {{- include "deepfacelab.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deepfacelab.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
